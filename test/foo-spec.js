"use strict";
const Foo = require("../src/foo");
describe("Foo", function () {
	beforeEach(function () {

	});

	afterEach(function () {

	});

	it("should ", function () {
		let a = new Foo();
		expect(a.foo()).toBe(4);
	});
});
